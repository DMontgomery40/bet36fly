# Current handoff: associative learning stage

**Documentation reconciliation, September 14 UTC:** Claude's session reports the one-shot 2018 job launched. No completed confirmation artifact was present in the local evidence/output directories at this wiki integration's cutoff; remote completion was not checked. Do not follow the older “held/untouched” wording as authorization to launch again. [Source cutoff and distinctions](evidence/wiki-integration-2026-09-14/reconciliation.md). Read the [organized research directions](../wiki/research-directions.md) for the fourteen memo items consolidated by topic; those proposals are not authorization for new runs.

**Authorized by David on September 13, 2026 (evening).** Implement and demonstrate dopamine-dependent synaptic learning in the actual MaleCNS circuit, then determine whether that learning improves sports prediction. Read [AGENTS.md](../AGENTS.md), [PROJECT_DIRECTION.md](PROJECT_DIRECTION.md), the [contract](EXPERIMENT_ASSOCIATIVE.md) and the [dated evidence index](evidence/associative-learning-2026-09-13/index.md) before continuing. The [natural sensory handoff](NATURAL_SENSORY_HANDOFF.md) is superseded; its result (the frozen 2023 confirmation) is unchanged and remains the product's only prediction.

## The four questions, answered separately

1. **Mechanisms implemented.** `bet36fly/associative_lif.cpp` / `associative.py`: full-graph Shiu-style LIF (active-set integrator, bit-identical to the sensory engine without reinforcer drive) at 0.11 mV/contact with three measured interventions; float32 multiplicative gains bounded [0.5, 1.5] on 3,417 eligible γ-KC→MBON05/MBON01 synapses; per-step rule η(D̄_c·K_j − K̄_j·D_c) with τ = 1 s traces, compartment-mean PAM08 (γ4) / PAM01 (γ5) dopamine spikes, per-compartment coupling (causal lesion switch); Poisson reinforcer drive of those DANs (engineered optogenetic-style substitute, because sweet taste evokes no dopamine spike at this coupling); explicit state lifetimes; checkpoints with SHA-256 lineage; probe cache keyed by the gain hash.
2. **Controlled conditioning.** Conditioning-01 (`associative-conditioning-42845736c62d3ac9841e`) failed its predeclared criteria and is preserved with its diagnosis. Conditioning-02 (`associative-conditioning-d1d3992e38c1dfa5fef1`) passed acquisition, retention, reversal, causal dependence (lesion/frozen byte-identical), timing (backward pairing potentiates), cue identity (swap), order and audit, with every verdict independently recomputed from saved artifacts. This qualifies the mechanism in this simulator only: engineered reinforcer, no natural sugar reward, no feeding, no extinction mechanism.
3. **Plasticity and held-out prediction.** Not established, now confirmed on the reserved 2018 block: the frozen recovery candidate (η 4e-5, ρ 0.005) beats chance (56.98%, interval 54.70–59.10%) but is reliably worse than its frozen twin (+0.000704 log loss, interval entirely above zero) and than the encoder alone, and equal to the shuffled and no-recovery arms. [2018 result](evidence/associative-learning-2026-09-13/sports-confirmation-2018.md). Earlier development evidence: On the 2022 development block the plastic circuit (η 1e-5, 2e-5, 4e-5) beats chance but not its frozen twin (paired intervals include zero) and is worse than the encoder alone (interval entirely above zero); the shuffled twin scores the same. Gains saturate at the lower bound during the season (14–44% of all selected gain slots at 0.5 by September). [Baseline result](evidence/associative-learning-2026-09-13/sports-development.md). The version-2 recovery stage subsequently passed its mechanism gates and was evaluated on 2022; see the completed recovery account below.
4. **Application.** The product's predictions remain the frozen sensory confirmation. `/api/associative/*` exposes evidence, explicit training jobs (start/cancel/resume, one writer) and probes of immutable checkpoints; the `/#learning` page shows the four answers separately. No checkpoint is promoted; `output/current-model.json` is untouched.

## Sports stage

Protocol: `configs/associative-sports-01.json` (development season 2022 around the frozen sensory pipeline; identical encoder, innate cache — shown invariant to the learned gains in [sweet-path-invariance.json](evidence/associative-learning-2026-09-13/sweet-path-invariance.json) — probe seeds and data across arms; winner odor + reinforcer applied only after the 48-hour/UTC-day availability rule; readouts fitted on games before July 1, 2022 with one fixed procedure for every arm; three declared learning rates). Reserved confirmation: the 2018 MLB season, fetched only by `scripts/run_associative_confirmation.py` after `scripts/freeze_associative_candidate.py` writes and the candidate is committed. **No-homeostasis baseline result (2022 development, circuit-01):** at every declared learning rate the plastic circuit does not beat its matched frozen twin (paired intervals include zero), is indistinguishable from the reinforcement-shuffled twin, and is reliably worse than the encoder alone; every arm beats chance. Between 14% and 44% of all selected gain slots sit at the 0.5 floor by season end, the bounded-weight saturation of Jiang & Litwin-Kumar 2021. [Full table](evidence/associative-learning-2026-09-13/sports-development.md). These arms are preserved unchanged as the baseline.

**Continual-learning stage (David, September 14):** the version-2 dopamine-gated recovery rule (contract §8, `configs/associative-circuit-02.json`) was qualified on Hugging Face Jobs: conditioning-03 at four ρ values and the stress test (`configs/associative-stress-01.json`, baseline versus recovery). The preregistered sequence required conditioning and reduced bound occupancy without destroying memory before a new 2022 recovery arm; ρ was chosen by those mechanistic criteria only. Result: conditioning-03 passed at every ρ, the stress test selected ρ = 0.005, and the 2022 recovery arms fixed the saturation (0.8–0.9% of gains at the floor) without changing the prediction verdict ([sports-recovery.md](evidence/associative-learning-2026-09-13/sports-recovery.md)); the candidate is frozen at η = 4e-5 in `configs/associative-candidate-01.json`. **The 2018 block is allocated to exactly one attempt** of the final recovery candidate against frozen, the no-homeostasis plastic baseline, shuffled reinforcement and the encoder/same-information baselines, and is never used to choose any parameter.

## How to continue

- Every code or protocol change is a new identity under `output/associative/`; never rerun an identity. Never reuse 2018 after its one confirmation attempt; never touch 2023.
- Mechanism gaps worth a bounded next step: the natural sweet→PAM pathway (two-hop anatomy exists, transmission absent at 0.11 mV/contact and runaway at 0.165–0.22); memory decay/extinction (none implemented; contingency swap leaves the old association); receptor-level dopamine kinetics (not modelled); the ALLN/APL sign proxies that required zeroing.
- Verification: `pytest tests/test_associative*.py`, `make verify`, rebuild `web`, `make serve-verify` plus the browser script for the `/#learning` page.
- Candidate directions after the 2018 result (visual accessory-calyx cue, optic lobe as encoder, central-complex in-circuit choice, prediction-error/punishment/multi-timescale learning signals, extra sensory channels, fitted circuit parameters), each with its finite first question and honest comparator: [ASSOCIATIVE_NEXT_APPROACHES.md](ASSOCIATIVE_NEXT_APPROACHES.md).

## Queue (written September 14, 2026, 05:50 UTC, while the 2018 confirmation was still running)

Ordered by dependency, then by the re-ranked roadmap in [ASSOCIATIVE_NEXT_APPROACHES.md](ASSOCIATIVE_NEXT_APPROACHES.md). Nothing below is authorized to run on neural data until David says so; items 0–2 are bookkeeping that the rules already require.

0. **Land the 2018 confirmation (blocking everything that makes a prediction claim).** Hugging Face job `6aa771a921047bf1b03862ae` (started 04:01:45 UTC, cpu-upgrade, about 4 h). Check with `hf jobs inspect 6aa771a921047bf1b03862ae`. When it stops, download the new `output/associative/associative-confirmation-*` directory from the private results dataset `dmontgomery40/bet36fly-results` (`hf download … --repo-type dataset`), read `manifest.json` first: `passed_confirmation`, `failed_confirmation` and `failed_runtime` all count as the one attempt. Verify the artifact SHA-256 map and recompute the verdict from the saved arm rows before quoting anything. Then, in one task: append the result to item 7 of the [evidence index](evidence/associative-learning-2026-09-13/index.md), rewrite question 3 above, update the README, model card and wiki reassessment, expose the confirmation through `/api/associative/*` (no confirmation endpoint exists yet) and give the `/#learning` page a card for it (the page currently has no 2018 slot; frontend work goes to a subagent). Never relaunch the job; if it failed at runtime, record the failure and stop.
1. **Wiki integration completed.** The [wiki index](../wiki/index.md) now routes to current mechanism, cell, conditioning, recovery, research-direction and evidence pages. All fourteen memo items are covered with overlaps consolidated and legacy findings scoped. [Coverage and corrections](evidence/wiki-integration-2026-09-14/reconciliation.md), [verification](evidence/wiki-integration-2026-09-14/verification.md), [static anatomy/storage audit](evidence/wiki-integration-2026-09-14/anatomy-and-storage.json).
2. **Documentation hygiene.** The [roadmap](ROADMAP.md) now points to this handoff and the integrated research directions. The unrelated `PUBLIC_POST_HANDOFF.md` remains a read-only older snapshot; David decides whether it is refreshed or dropped. Server shutdown is outside this documentation task.
3. **Reserve a fresh block before any new prediction claim.** 2018 is spent after item 0 and 2023 is never reused. `scripts/run_associative_confirmation.py` hard-codes season 2018; generalise it (season parameter, same one-attempt manifest discipline) so 2017 or earlier can be preregistered for exactly one candidate. Only pregame information; the MLB Stats API fetch happens inside the script, never by hand.
4. **Item 5 of the memo, in parallel and cheap: extra pregame streams into a conventional model only.** Run differential, streaks, rest and travel added to the same-information baseline on the 2022 development block, zero neural calls. A negative screen would stop that bounded proposal under its declared rule; it would not prove that every model or circuit must fail on those inputs.
5. **Roadmap A: richer readout from the saved 2022 checkpoints.** Re-probe every team cue at every saved UTC-day checkpoint (179 per arm) with a wider recorded channel set (all relevant MBONs, MBON→downstream convergence cells, fan-shaped-body targets), plasticity off, about 6,400 probes per arm on Hugging Face. Same fixed logistic procedure, frozen and shuffled twins, encoder-only comparator. New identity; development data only.
6. **Roadmap B: fixed temporal reservoir.** Sequence-driven state over pregame history, one small frozen readout; controls are the flattened conventional model, a matched echo-state reservoir, the degree-preserving rewired MaleCNS (v2 null-graph machinery) and a random graph. Anatomy gets credit only if MaleCNS beats all three nulls.
7. **Roadmap C: residual / surprise reinforcement.** Dopamine proportional to outcome minus the frozen pregame probability, negative residuals through a PPL1 punishment compartment. Requires a new conditioning battery (identity, timing, lesion controls unchanged) before any 2022 arm; the online conventional residual learner is the decisive comparator.
8. **Roadmap D: interference and credit assignment, artifacts first.** KC-overlap versus unintended gain change across the 30 team cues and the conditioning cues, edge-sharing histogram, regression of the retired cue's drift on shared-edge count. Remedies (sparser codes, orthogonal cue families, compartmentalised memories) are each a new identity.
9. **Memo item 13, before E–G: physiological calibration with no sports fit.** Replace the engineering interventions (ALLN out 0, APL out 0, DA fast out 0, 0.11 mV/contact) with source-bound parameters, frozen before any evaluation. The full graph is already stepped, but visual/CX-specific stimulation and computation have not been qualified; the ALLN/APL sign proxies required interventions in the measured odor assay.
10. **Roadmap E, F, G in order:** visual accessory-calyx learning (links first), central-complex in-circuit choice (swap-reversing PFL asymmetry with plasticity off), full sensory-to-motor choice. Mechanism claims, no block needed; prediction claims need item 3.
