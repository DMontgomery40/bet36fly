# Output-only rate-adaptation shadow implementation

September13,2026 UTC. Implementation and tests are frozen for independent boundary review. No saved real history has been evaluated with the candidate. All work is under output/collaboration/reward-mechanism-repair; no production/kernel/UI changes or native calls occurred.

The pure calculator integrates the fixed positive rectified DAN rate contrast and its eligibility, preserves zero-time history with writes from100ms, splits mathematical integration at the exact contrast crossing, and completes the infinite tail. Shared KC states advance once over the whole interval independently of channel order. Double gains publish float32 once per complete0.2ms interval and once for the full tail. It records separate true positive/negative areas, attempted/double-applied/published changes, inclusive bound observations, all four phases, all8866edges, class/channel groups, onset/endpoint states and a peredge absolute-area excursion proof. Learning-off and excluded edges retain their bytes.

The execution boundary accepts only the fixed32ordered cue/noise/seed rows, fixed parameters, root-preregistered manifest and a successful hash-matched numerical/boundary review. The inspect-only manifest builder binds all32fine NPZs,64cold/continuous comparisons, sample maps, capture summary/receipt, scientific/numerical sources, calculator/tests and independent oracle/review. Exact verified bytes are parsed; symlinks, missing/changed files, nonfinite/object arrays, unsafe ZIP members and mismatched NPY-declared allocations fail closed. Each real row first reproduces both unadapted comparators from its actual history before candidate evaluation. It checks actual parent checkpoint bytes, full masks, shared KC state and peredge area domination, then rehashes every input before completion.

One deterministic output directory per manifest prevents an existing identity from running elsewhere. Attempts and completions are exclusive perrow files, and failures retain partial artifacts. The600-second cap spans validation, comparison, candidate computation and output; candidate code checks it every50steps. No candidate result feeds a circuit. A passing necessary stability screen remains explicitly qualification=false.

Meaningful pre-run fixes included correcting the initial synthetic class/channel mapping to actual channel-major (four class slots per channel) groups, removing channel-order-dependent KC rounding, rejecting finite-input numerical overflow, and rejecting forged NPY shapes before NumPy allocation. The reviewer demonstrated a header-only800MB declared allocation; the regression family also covers integer-product overflow, truncated and extra payloads plus valid scalar/empty/Fortran numeric arrays. A finite-pulse test initially checked before the actual zero crossing; its intended post-crossing assertion now waits300ms, preserving the specified equations.

Fresh combined synthetic gate:373passed in11.88seconds (144writer tests,63independent oracle/analytic tests,166direct independent calculator checks). Ruff passes. Writer tests include an additional high-accuracy ODE check using a finite20000ms continuation; the authoritative infinite-tail proof is the separately written70-digit Laplace quadrature and direct reference suite, not that finite approximation. No test tolerance was adjusted to fit a real outcome. Root owns the full repository gate; this output-only agent did not run a gate that could invoke native code.

Release calculator SHA256:bad6425c1b54b0f48b04b8238be3e426372d3a8a128838bb2260f9d93b5652e8.
Release tests SHA256:daf769f31cacd3519beafcacd163de946fbc9e6c94954d940fcaba24094466c7.

Commands:

```text
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q output/collaboration/reward-mechanism-repair/test_rate_adaptation_shadow.py output/collaboration/reward-mechanism-repair/rate_adaptation_reference_tests.py output/collaboration/reward-mechanism-repair/rate_adaptation_reference_writer_tests.py
.venv/bin/ruff check output/collaboration/reward-mechanism-repair/rate_adaptation_shadow.py output/collaboration/reward-mechanism-repair/test_rate_adaptation_shadow.py
```

The CLI's --build-manifest option reads/hashes inputs only and emits draft-not-authorized. Root must supply the reviewed oracle/test/contract and combined approval paths, then freeze the complete receipt. Execution additionally needs its exact SHA256 and the new output path rate-adaptation-shadows/rate-adaptation-shadow-<first20manifesthash>. This report grants no execution authority.

Final independent boundary review:PASS on the released calculator/test hashes,37focused tests and all97real input archives checked with candidate disabled; hashes unchanged. The numerical reviewer is binding its final recheck to the same release. Root still owns combined approval, freeze and any actual execution. The600-second timer uses cooperative checks around bounded Python/file operations; it does not promise forced interruption of stalled OS I/O.
