# Independent helper, exact guard and runner review

September 13, 2026 UTC. Reviewer: evidence_ui. No remaining actionable finding
in this bounded preparation review. This is not a saved-history result, a
qualified mechanism or demonstrated conditioning. Root owns the final plan
freeze, source audit and any later dispatch.

The helper implements the fixed gain-dependent equation with continuous double
gain, exact filtered signal evolution between impulses and a complete tail.
The independently derived eight-trial guard remains `9 S^2 <= 16 Q`, where
trial totals are exact float32 ticks. The conservative box classification and
possible-bound counters remain conditional on the declared numerical allowance;
passing software tests is not a formal global integration enclosure.

A synthetic extreme-product probe independently reproduced a finite gain with
NaN accumulated areas before the helper's rejection-only correction. Six
independent scalar/vector overflow cases now reject, while three very large
finite-integral controls remain valid. No real capture overflow was observed.

Seven independent selector mutations exposed the initial runner's incomplete
identity check: duplicate trial identity, wrong game, wrong seed, wrong panel
run ID, float seed, bool panel and within-cell permutation. The corrected runner
validates all 32 ordered semantic selectors and compares them with the bound
capture. All seven regressions now pass.

Code review also found completed rows counted before their detail and journal
outcome were persisted, and a finalization path that could outlive the cap while
retaining completion. Completed counts now advance after both writes and the
journal flush. A computed summary needs a separate hash-bound completion record,
all 64 journal entries and all 32 artifact/detail pairs; any terminal-error
marker invalidates completion. The alarm remains active through terminal writes
and post-write clock checks. The CLI also invalidates its screen after terminal
failure. Tests inject source/helper/row/summary/completion failure and cap
crossings during both terminal writes. Successful exclusive creation and refusal
to reuse the same identity are covered with labeled synthetic arrays.

Verification covers **539 distinct synthetic cases**: 432 existing numerical,
helper and guard tests, 45 independently authored reviewer tests, and 62 runner
contract/lifecycle tests. The pure review ran 470 cases in 16.47 seconds; the
final runner/reviewer run passed 107 in 11.51 seconds. These executions overlap
on 38 reviewer cases, so their counts must not be added as distinct evidence.
The reviewer test passes Ruff. No real study prepare or execution, saved-history
candidate evaluation, native or neuronal call, network request, production edit,
browser/server run or commit occurred in this review. Runner integration tests
substitute temporary synthetic maps, captures and helper outputs explicitly.

The metadata copy closeout is separately complete: nine files totaling
16,890,068 bytes, with source and copied hashes matched and originals preserved.
The complete run subdirectory and filenames remain intact. The copy manifest,
pure review receipt and final reading/review receipt preserve the exact scope
and identities. Shared narrative files were read, not edited, by this reviewer.
