# Frozen source-only Handler protocol verification plan

Declared September 13, 2026 before any six-protocol execution. This tests the authored Gkanias et al. 2022 executable; it introduces no BET36FLY candidate, fit, parameter transfer or physiological calibration. Only synthetic tests may run before root reviews the helper and this plan.

Source is the six unchanged files in `incentive-circuit-source-2026-09-13`, pinned by `fetch-receipt.json` to commit `1610c80072fe8bb59bd397e7a61f716393a509b9`. Root independently verifies the publication archive. Preserve the complete GPLv3 LICENSE and every original header, including the handler/driver's existing MIT labels; do not rewrite this mixed provenance. The separate audit helper is marked GPL-3.0-or-later and identifies its independent reproduction role.

The six authored cases are ISIs `[-6,-1.2,-0.6,0,0.5,6]` seconds, CS `[0,0.5)`, US `[ISI,ISI+0.6)`, on the exact executable `np.linspace(-7,8,1001,endpoint=True)` float64 grid. Its interval is 15 ms (not 30 Hz, 10 ms or native 0.2 ms). Preserve the executable's floating endpoint comparisons; do not silently substitute an ideal rational mask. Driver constants are tauKC=100/3, tauShort=60, tauLong=104 **samples**, including the first-sample initialization; run_case's other defaults58/100 are not the target. No additional dt multiplier enters its W update.

The independent scalar recurrence preserves old-state dependencies: update/clip K, compute current MBON from K and prior internal W, update/clip both DAN filters from US minus **previous** MBON, then update unbounded internal W by `(D2-D1)*(K+W-1)`. Recorded w is `max(W,0)`; it is not the internal state. Capture actual source internal W by an observational wrapper around the original plasticity function without modifying upstream files. Compare all1001 samples of CS,US,K,D1,D2,MBON,recorded w,dR1,dR2 and internal W against the scalar calculation, at fixed atol/rtol1e-12. Time and stimulus arrays must agree exactly. Preserve discrepancy arrays and failure status rather than adjusting tolerances.

Synthetic tests will discriminate update order, first-sample behavior, separate time constants, lower/upper signal clipping, internal-negative W versus rectified recording, DAN-only changes away from unit W, unit-W invariance without KC, absent-DAN invariance, and unsupported/nonfinite inputs. Analytic unclipped impulse/step filters check amplitude, DC gain, crossing and complete-tail area. A separate exact-tail argument distinguishes zero full-cycle signed filter area and continuous passive recovery reversal from the authored discrete Euler product. The original finite8s record must not be reported as infinite continuation.

Reporter outputs remain separate: pointwise `-dR1-dR2`; the driver means `-dR1` on `[-7,1)` and `dR2` on `[ISI,ISI+4)` after the source time offsets; independent min-max normalization across six conditions; normalized ER-minus-cAMP contrast; actual final internal W and recorded w. These quantities are not interchangeable. No experimental workbook or correlation claim is part of this helper.

After review, root may dispatch exactly one child calculation with a30s wall cap, six authored cases and six scalar comparisons. No native/CNS calls, saved BET36FLY inputs, network, plotting, fitting, sweeps or automatic retry. Use an exclusive identity directory, persist a started receipt before calculations, per-case arrays and progress after each completed case, and a terminal completed/failed receipt. Rehash all six sources, plan and helper before/after; any mutation, mismatch, timeout or persistence failure invalidates completion. Time covers imports/verification/calculation/output; root also owns an external watchdog. A finite-tail or diagnostic synthetic example is never appended to the six authored protocols.

Planned files: `audit_incentive_handler.py`, `test_audit_incentive_handler.py`, final preparation/reading receipt. Final source-protocol execution remains pending root dispatch; synthetic mathematical checks cannot establish MaleCNS learning or qualify a circuit mechanism.
