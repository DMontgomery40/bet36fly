# Task4B artifact evidence/API/UI implementation report

Agent evidence_ui; production/tests/browser script frozen for root review. No local commit, server/browser launch or neural experiment.

The Training workflow now shows mechanism qualification, conditioning/reversal status and historical sports separately. One experiment poll feeds reward/conditioning/v2 views; diagnostics refresh independently with retry and stale-data labels. Original bridge de050d773763 is validated passed, second dea14759e9ca validated failed; raw original/second remain stored-only overall because replay/sensory evidence is incomplete. No conditioning result is fabricated.

The new pure artifact reader validates immutable summary/trial locks, exact preregistered selectors, complete matrices, all seven criteria, bridge layout/checkpoint/gain/bound reconciliation, full replay fingerprint schemas and actual sensory hashes. Full scientific pair identity includes native binary and common selector data. Missing/invalid/partial data and declared corruption stay explicit. A byte-hash-keyed in-memory cache avoids repeating NPZ reconciliation but rehashes relevant files every read; replacement, deletion and symlinking invalidate prior passes. Full JSON detail returns matching validation and never serves raw NPZ.

Concrete defects corrected in the inherited draft: pytest ApproxMapping union, skipped declared corruption without an external lock, incorrect bridge layout keys/field labels, ignored false completeness return, entire-selector removal during pairing, omitted native binary identity, incomplete replay dictionaries, array-shape exceptions and missing gain-delta reconciliation, and preregistered-but-unfinished directories mislabeled invalid. Provenance labels now require inspected preregistration. Declared corruption is checked even when completion is absent.

Verification: focused backend/API 47 passed before widening metadata values with two existing deprecation warnings; fresh make verify passed 970 Python tests, Ruff, 81 Vitest tests and production build. Browser script syntax passes. The browser acceptance script now checks actual mechanism rows, exact per-channel guard values, all seven criteria, bridge/tail fields, JSON consistency, blocked qualification/conditioning, responsive widths, and labeled synthetic loading/empty/error/partial/running/cancelled/budget/unknown states. Root must execute it on the read-only factory with file-integrity proof. No browser success is claimed by this report.

Conditioning scope remains incomplete scientifically and operationally: the retained draft needs its frozen call-plan/state/replay/bounds fixes, actual runner, independent artifact validator and qualified mechanism before execution. Future generic manifests display stored progress, criteria and ancestry with an explicit unverified scientific verdict. Complete jobs or stored booleans cannot become a pass. No native/protocol/evaluator or scientific defaults changed.

Files owned by this implementation:
- bet36fly/reward_evidence.py; bet36fly/server.py
- tests/test_reward_evidence.py; tests/test_api.py (preserving inherited verification-mode assertions)
- web/src/RewardEvidence.tsx; web/src/RewardEvidence.test.tsx
- web/src/ExperimentRuns.tsx; web/src/ExperimentRuns.test.tsx
- web/src/RewardExperiments.test.tsx; web/src/TrainingView.tsx; web/src/types.ts; web/src/style.css
- scripts/verify_reward_browser.cjs; docs/api-contract.md

Root-owned docs/wiki/evidence and atlas edits are excluded from this ownership report. Complete reading receipts and inventories are evidence-ui-resumed-reading.md, evidence-ui-reading-delta.json and evidence-ui-final-reading-delta.json in this collaboration directory. Full gate log: task4b-verify.txt.

## Root-review corrections and final release

Root review requested malformed-metadata, race and stale/retry corrections. Regression tests reproduced the mixed numeric/string created_at sorting crash, empty bridge-contract SSR crash, valid-JSON 1e400 overflow serialization failure, summary/lock replacements between parsing and hashing, and detail/index inconsistency during replacement. Literal NaN/Infinity were already rejected; they are not represented as newly reproduced failures.

The reader now rejects nonfinite values recursively (including exponent overflow), validates rendered creation/protocol/bridge metadata, and reparses summary/locks inside before/after content snapshots. Cache hits also check the ending snapshot. Detail reads reject concurrent replacement. The conditioning section exposes its own stale registry warning. The browser script now exercises an actual failed-fetch Retry→success interaction and successful→retained-stale→recovered diagnostics and conditioning, using labeled isolated response fixtures and a controlled page clock.

Final full make verify: 938 pytest tests, two existing deprecation warnings, Ruff, 81 Vitest tests and rebuilt production bundle. Focused backend/API33 passed. Browser script syntax and git diff --check pass. Production/tests/browser script are refrozen for root review and real browser/file-integrity acceptance.

## Independent-review corrections

The reviewer and this agent reproduced synthetic within-compartment cancellation that hid out-of-bounds trial gains and per-group publication mismatch. This was malformed-artifact validation coverage, not a defect observed in the pinned measured archives. Each bridge trial now checks eligible endpoints and contacts, group mapping and groupwise published changes. Valid zero-effect and correctly reconciled cancelling groups remain accepted.

The metadata boundary now rejects every rendered string/hash field when supplied objects, arrays, numbers or booleans, including DAN reference, mask, tail/layout, native digest and source digests. The actual endpoint payload is rendered by the actual RewardEvidenceView using existing esbuild/React for every malformed family; this is real API-to-SSR integration, not a duplicated mock renderer. Original-only, held-out-only and both-member failures now name exactly the failed diagnostic members in conditioning blockers.

Final gate after independent corrections: 970 pytest tests, two existing deprecation warnings, Ruff,81 Vitest tests and production build. No server/browser/scientific experiment ran. The complete release remains pending root's real browser and protected-file acceptance.
