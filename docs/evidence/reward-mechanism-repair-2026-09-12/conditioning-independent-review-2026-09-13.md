# Independent conditioning contract and artifact review — September 13, 2026

The bounded review has no remaining actionable findings in the corrected
families below. This is software evidence. The real second mechanism panel
still fails its untaught-home guard; no MaleCNS conditioning or reversal was
performed. Parent integration, the complete repository gate and browser
acceptance remain separate responsibilities.

## Contract and numerical expectations

The independent oracle derives its ordered rows and expected values from the
original conditioning preregistration, scientific addendum, task-3 design and
test-oracle documents, and the implementation clarifications. It does not call
production plan or arithmetic helpers to obtain expected values. The actual
production plan was then compared with that independent oracle: all 1,632
ordered semantic rows match, including 616/616/400 stages, 32 replay references,
all 16 acquired-parent probes before any reversal training, seed/window/pulse
identities and checkpoint ancestry.

The independent oracle's 1,356 tests include valid controls and generalized
mutations of order, identity, schedule, specificity, frozen responses, gains,
reversal recovery and sufficient ordinary remapping. Those tests check the
oracle's expectations and are not counted as 1,356 executions of the production
validator. The preserved old draft differs in 13 of 14 documented comparisons;
the remaining comparison is a valid acquisition control. Its original and
tracked paused-draft copy remain byte identical at SHA256
`4f30fd52cfa7de8350876961465e66cb6e131859799be8f87fcd320310eb3903`.

The separate 1,224 arithmetic cases do exercise the actual production
acquisition/reversal evaluators. They cover MBON population denominators 2/4,
count scales 1/7/29, both panels/channels, all four response seeds, exact
threefold equality and adjacent single-count failures. Float32 gain cases use
preferred-set sizes 3/7 and 7/3 and one-ULP neighbors. Expected comparisons use
integer sums or exact rational values. New-association ratios use four-seed
means; old-response recovery remains per seed. Ordinary remapping can preserve
or further depress the old response/mean gain while individual old edges
redistribute. Partial recovery is not promoted to the sufficient no-old-recovery
classification.

## Qualification and saved-artifact boundaries

A disposable, explicitly synthetic original/held-out diagnostic pair exercises
the actual diagnostic artifact validator and actual conditioning qualifier.
The measurements are manufactured; real graph, mask and scientific-file
identities are bound only as immutable inputs. The accepted control binds 46
files. Large inputs are temporarily hardlinked and never written through;
small inputs are copied. No engine is constructed, and the temporary diagnostic
folders are removed. This is an API acceptance fixture, not a measured passing
mechanism or a physiological simulation.

All 15 qualification expectations passed: initial/restored positive controls,
approved native/source/graph/pilot path restrictions, accepted APL/KC/global
gains, current scientific source hashes, document binding semantics, and
replacement of trials, summary or replay bytes after the diagnostic reader
returned a pass but before qualification captured its bindings. The final
three cases now reject the replacement rather than inheriting the earlier
validation. A changed conditioning document receives a new identity binding;
a bridge source document must still match the qualifying mechanism identity.
The earlier v1 probe mistakenly added an unused `weight_scale` key; v2 and v3
correctly mutate `global_weight_scale`. That probe mistake is not reported as
a production gain-control defect.

The corrected numerical/container/compact suite passed 1,452 cases: the 1,224
arithmetic cases plus 216 schema/gain/container cases and 12 compact count
cases. The latter include valid ceiling and sampled-subset controls as well
as impossible cue/trial counts, sample-versus-population contradictions,
zero-DAN tonic contradictions and population-bin ceilings. Earlier false
acceptances of malformed numerical products, missing publication, frozen
changes, nonfinite/type/schema families and duplicate JSON keys are preserved
in the output review history; current generalized tests reject them.

One shared complete 1,632-call FakeEngine artifact fixture supported 28
terminal-disposition/time-cap checks. Only known completed disposition,
all reconstructed criteria and wall time below 1,200 seconds can pass.
Budget-stopped, cancelled, failed, running and unknown dispositions cannot
inherit a scientific pass. Two additional snapshot tests replace an already
consumed checkpoint or an approved synthetic source after the last compact
artifact has been read. Both are rejected. These tests substitute the
qualification/input-preparation boundaries explicitly; the separate real
qualification fixture above covers those boundaries without substitution.
FakeEngine is ordinary deterministic Python test code and makes zero native
calls.

## Evidence limits and closeout boundary

All calls retain complete numerical fingerprints and compact arrays; the
selected 32 originals and 32 replays retain complete raw numerical outputs.
The reader recomputes response/KC/gain criteria from retained observations.
It cannot reconstruct omitted raw traces from hashes or individual transient
synaptic paths from group totals. Native electrical and analytic-tail bound
observations remain separately instrumented evidence, supplemented by final
per-edge gain checks. No new per-edge trajectory, causal ablation, local
exposure coefficient or counterfactual firing claim is made.

The reading receipt inventories all current narrative docs/wiki and links the
prior complete reading chain. Newly read material includes the 06:00 handoff,
updated Training/API explanation, all three bounded localization reports and
the offline partners-only selection synthesis. No body values or coordinates
were retrieved by this review. The localization evidence does not alter the
home-all/away-gamma masks or resolve mechanism qualification.

The accompanying JSON receipt records exact source/test/report identities and
standard-suite copies at its final boundary. Historical probe logs remain
unchanged. Their earlier mutable writer states were not retrospectively
reconstructed as frozen releases. Parent owns final make verify, rendered
browser acceptance, protection checks and the local commit. This reviewer
made output-only edits and zero native/full-CNS calls.
