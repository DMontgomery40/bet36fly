# KC-local calcium: root preparation and review

September 13, 2026. The full repair goal remains incomplete. This preparation
selects and implements an output-only hypothesis; it does not qualify a live
circuit. Production, API and Training still describe the existing failed
rate-bridge qualification accurately.

The frozen candidate contract (`342c25f8…`) retains the primary model's 30 Hz
old-state C/L/a/I equations and multiplies actual KC impulses by held L/C
before both existing plasticity filters. One spike is one source-input unit
for its completed frame. That scale, use of L/C as susceptibility and transfer
from gamma physiology to all 4,064 KCs are declared engineering assumptions,
not parameters identified for these cells by the external population data.
Zero C=L gives susceptibility one. The external model's shock gate and
depression-only learning law are not part of this candidate.

The separate lateral map uses 642,932 incoming normalized off-diagonal pairs
and 1,153,844 contacts. Its single excluded self-contact remains in the fast
electrical graph. All 4,184 home edges remain eligible; away retains 3,239
eligible gamma edges and 1,443 unchanged transmitting edges. No electrical
gain, bridge constant, onset, mask or acceptance criterion is adjusted.

One external fit, `kc-external-refit-992cef6fb05bfdb63d2b`, completed in
2.324875 seconds under the declared 120-second parent watchdog. KD and WT
stages used 210 and 245 actual objective evaluations. The independent auditor
reproduced all 259 fitted source-model samples with maximum mean errors
8.88e-16 and 4.44e-16, and independently checked all 499 workbook rows.
The WT standard-error-weighted RMS residual is 2.12006, with a maximum
weighted positive residual of 17.65335 at 3.67 seconds: the numerical
reproduction does not imply a uniformly close physiological fit or a unique
optimum. A numerical-derivative warning from an invalid intermediate domain
is preserved in stdout; final parameters are finite and separately checked.

The fit uses the inspected primary source pinned at
`nawrotlab/KC_KC_lateral_interactions@f0ee2079dae6761cc2d07f04e96e76e2654b6e3c`.
The [official L-BFGS-B documentation](https://docs.scipy.org/doc/scipy/reference/optimize.minimize-lbfgsb.html)
and [curve_fit documentation](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.curve_fit.html)
were checked September 13, 2026 (rendered manual 1.18.0; local SciPy 1.18.1).
Numerical derivatives may require extra function evaluations, so the helper
counts actual calls independently of solver-reported nfev/maxfun. No parameter
search uses these saved MaleCNS histories or sports outcomes.

Root reviewed the local operator, anatomy construction, weighted bridge,
composition tests, fit helper and independent source audit, and the runner's
full lifecycle. The combined changed-surface suite passed 532 cases in 3.43
seconds before saved-history execution. The complete repository gate also
passed: 4,055 Python tests, 85 frontend tests, Ruff and Vite. Two existing
Python warnings remain. All 791 protected files / 5,497,884,387 bytes matched
their prior inventory after that gate. No browser run or visual inspection of
the supplementary fit SVG is claimed.

The next calculation is exactly the frozen 32-history screen, once the
independent auditor has passed its own synthetic checks and root review.
Use one direct child with a 1,200-second external watchdog; do not restart.
Preserve all eight unchanged guard cells, every trial and all invalid/bound
outcomes. A favorable result permits an independent saved-result audit only.
Both complete live circuit panels and controlled acquisition/reversal remain
required before any learning claim.
