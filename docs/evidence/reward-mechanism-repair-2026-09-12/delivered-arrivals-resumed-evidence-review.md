# Independent evidence API/UI review — initial findings

September 13, 2026 UTC. Reviewed released reader `377e6bec5bda0484864e9d4b038e3036e86144bb58a54c02ae79d326a54d471b`, component `1b9400fa0a818b736ca6bf17ef7f7178a2eba0c29a39a63f0998ae8a7cec2f71`, the associated server route diff, types, actual backend/component tests, shared polling/view integration, and browser verifier diff. The full release manifest is `task4b-release-hashes.json`. Root subsequently authorized the sole writer to resolve findings; this report is the pre-correction record, not a final verdict.

1. **[P2] Compartment cancellation bypasses group and endpoint validation.** `bet36fly/reward_evidence.py:332` `_bridge_reduction` accepts two eligible home gains `[0.375,1.625]` from unit gains, zero rule/tail arrays, exact stored deltas, group indices `[0,2]`, and zero reported bound counts because the compartment sum is zero. Lines 355–367 compare only compartment sums; the groups argument is unused, and individual trial endpoints are not independently bound-checked. `_recompute_common` checks only the final cumulative checkpoint. A pure Python/NumPy call reproduced acceptance; the implementation agent independently reproduced it. Add per-group publication reconciliation, aligned group/mask/class validation, and inclusive endpoint checks on every trial, with valid zero-effect and cancellation/both-bound/misgroup counterexamples. This does not allege that the immutable real archives contain the corruption.

2. **[P2] Malformed presentation fields can crash the whole evidence component.** `_compact` forwards object-valued `dan_reference`, `away_plasticity_mask`, `native_binary.sha256`, and nested `identity.code_hashes` values as stored-only metadata. The actual `RewardEvidenceView` renders them as React children. Four isolated API-result fixtures, bundled actual component code and React server rendering each produced `Objects are not valid as a React child`. Results are `delivered-arrivals-resumed-evidence-review-ui-probes.json`; fixture roots live under the matching `-fixtures/` directory. Extend the output contract across all rendered scalar/hash fields, keeping a malformed row visible as invalid and preserving healthy sibling rows. Root's earlier created_at/nonfinite/bridge-contract fixes were read and were not refiled as new findings.

3. **[P3] Blocking diagnostic IDs assume only held-out can fail.** `list_reward_evidence` takes the held-out ID from every failed pair. If only the original member fails, the explanation names a passing panel; if both fail, it omits one. Derive blockers from actual failed member entries and cover original-only/held-out-only/both failures. The present stored bridge pair is correctly named; this is a generalized state error, not an incorrect current result or a demand for an unimplemented conditioning-pass validator.

## Coverage and limits

Read all original narrative docs/wiki and subsequent additions before review, including the frozen Task 4 design and current API contract. Applied the thorough-code-review correctness/test-integrity workflow within root's explicit read-only scope; skipped its modifying GitNexus bootstrap and production-test edits. No server or native engine was started, and no production/test file was edited by this reviewer.

The real pure reader returned the original bridge as validated passed, second bridge as validated failed, both raw panels stored-only, and conditioning not run due to the second bridge guard. Confirmed that reading did not import `bet36fly.reward_brain`. Reviewed exact selector pairing, immutable artifact locks, mask/frozen preservation, trial/cumulative/tail arrays, inclusive counters, replay fingerprints, hash cache and concurrent replacement checks, API detail behavior, loading/empty/error/stale UI, retry browser scenarios, and retained historical sports/v2 separation. The parent owns actual browser acceptance and the repository-wide gate.

No fake or gamed tests were identified. The existing tests were meaningful but omitted the concrete corruption and field-shape families above. Conditioning remains unrun and cannot acquire a scientific pass from registry flags; no future conditioning artifact validator was demanded in this review. Final PASS depends on reviewing the covering corrections and their verification under a new release hash.
