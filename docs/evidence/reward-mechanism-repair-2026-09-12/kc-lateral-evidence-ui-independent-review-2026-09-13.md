# Independent KC lateral source-semantic review

September 13, 2026 UTC, at BET36FLY `ab0f80f`. The root audit's algebra and its warning about timestep transfer are correct. **139 synthetic tests pass: 57 root cases plus 82 independent cases.** No fit, plotting/learning driver, native engine, circuit, saved spike history or network operation was executed. The current scientific gate remains unchanged.

Personally read the complete pinned upstream core, `fit_functions.py`, `fit_rate_model_to_data.py`, `learning_rate_screening_mbon_normalized.py` and README, plus root's audit protocol/test and the source review. The two executed function modules match their predeclared Git blob identities. The source repository commit is `f0ee2079dae6761cc2d07f04e96e76e2654b6e3c`.

The stored-state recurrence really is `I_next = f * ((1-r)*I + r*B)`, where `r=dt/tauinh`. Solving the fixed-point equation gives `I*=f*r*B/(1-f+f*r)`. For fixed `0<f<1`, the limit as dt approaches zero is zero; f=1 instead gives I*=B. A continuous relaxation toward fB is a different equation. The source uses the recipient's previous axonal calcium to compute f and multiplies the entire newly calculated inhibition state, while its calcium update uses the previous inhibition. The fit's lateral matrix is uniform, excludes self-connections and normalizes each recipient row before multiplying by the global inhibition factor. It is not the MaleCNS contact matrix.

The independent suite closes concrete coverage gaps in root's initial 57 cases. It includes the actual **0.01 s learning timestep**, in addition to 1/30 s fitting and 0.0002 s electrical steps; an asymmetric matrix and neuron permutations to distinguish recipient rows from presynaptic columns; heterogeneous 2/3/5-unit multi-step WT trajectories with separate calyx/axon/adaptation/inhibition states; positive-shock, zero-shock, overshooting prediction and residual DAN state; and the learning script's previous-D rather than newly calculated-D update. Tiny scalar expectations use explicit neighboring-cell sums and previous-state variables, not the root audit's oracle.

One concrete upstream reproducibility caveat is now pinned by a 12-case synthetic family. `fit_rate_model_to_data.py:195` first multiplies its residual-derived `n_str` by `sqrt(2*dt/tauKCdec)`, then passes that value to the noisy function calls at `:209` and `:211`. The functions apply the same scaling again (`fit_functions.py:57`, `:146`). Thus that demonstration's effective coefficient is sigma times `2*dt/tauKCdec`. By contrast, `learning_rate_screening_mbon_normalized.py:34` scales once and adds that coefficient directly at `:175`. The tests hold dt and parameters fixed to isolate this composition. This does **not** affect the noiseless optimization calls or establish that a published physiological result is wrong; it must be resolved explicitly before treating noisy reproduction paths as equivalent. Upstream files were preserved unchanged.

The driver facts are explicit: fitting uses 30 Hz and a five-second odor (`fit_rate_model_to_data.py:41`); learning sets dt=0.01 s and labels tauDAN=2 s among parameters not fitted to data (`learning_rate_screening_mbon_normalized.py:29`). It sets a 60-second odor and 12 shocks, each 1.25 seconds with 3.75-second gaps (`:43`). These are not the present 400 ms BET36FLY trial or a measured PPL101/PAM12 kinetic calibration. The weight update uses previous calcium and DAN state (`:258`), is purely depressive for nonnegative inputs, and has only a zero lower clip. From cold zero DAN, no shock prevents new DAN drive even if the model's prediction error is nonzero. Residual DAN after a shock can continue depressing weights. Neither case is an independent endogenous-DAN null-drift demonstration.

Concrete transfer choices remain open:

- Decide whether the additional variable changes electrical KC activity, axonal calcium/output, plasticity susceptibility or separately specified effects. The published numerical model has distinct calyx and axon calcium states; our current LIF has one membrane and one fast input state per neuron.
- Define units and a mapping from spikes/local membrane state to normalized calcium. The source sigmoid acts on normalized activity; the reviewed oocyte static concentration-response assay is not that mapping or a receptor kinetic ODE. No claim of personally rereading the inaccessible Methods S1 supplement is made.
- Choose a declared timestep/state-lifetime interpretation. Retaining a fitted discrete multiplier at another dt and replacing it with a continuous receptor law are different mechanisms. Specify reset and post-input continuation rather than silently transferring them.
- Specify lateral anatomy and receptor support without flipping all ACh fast edges or deleting contact data. The source's uniform row-normalized 700-KC population is not the retained 4,064-KC MaleCNS graph. Gamma-KC evidence does not by itself assign parameters to every eligible home class.
- Keep DAN input and teaching distinct from the source's shock-gated prediction-error proxy; do not copy that gate to make untaught activity trivially neutral. Keep fixed BET36FLY gains, eta, readout and controls. The source's LTD-only rule does not provide the separate potentiation/recovery branch required for the frozen strict reversal result.

The source agent's physiology/model review accurately separates applied DA/ACh solution assays, oocyte static EC50 measurements, normalized calcium model parameters and missing body-specific calibration. This review checked that written account against the inspected numerical source; it did not fetch primary pages or fit parameters.

The independently authored coupling report separately records 87 analytic synthetic tests, covering the timing kernel, exchange/reset/pooling identities, scale-invariant guard and abstract opponent-error algebra. I personally read that complete report; I did not rerun its suite. Those 87 tests are a distinct proof level from the 57 plus 82 direct source-function tests, and neither is an external-data fit or a MaleCNS candidate result.

Final targeted verification: 139 passed in 0.14 s; the independent file passes Ruff. An earlier combined run also passed 139 cases; Ruff then identified an ambiguous local variable name, which was renamed before formatting and the recorded final rerun. No scientific behavior changed. Tests and logs are output-only. The source acquisition's 225.688-second duration exceeded its requested 120-second cap; the existing terminal-process and budget-exception receipt remains intact, with no retry. Passing these source-semantic tests authorizes no candidate transfer or conditioning run.
