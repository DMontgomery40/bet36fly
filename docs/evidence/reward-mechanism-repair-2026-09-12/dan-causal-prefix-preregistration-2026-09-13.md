# Existing-record DAN/KC causal-prefix audit

September 13, 2026 UTC, starting checkout `dce1582`. This is one offline data
analysis of the current pooled rate-bridge records. It is not a plasticity
candidate, neuronal replay, new qualification or conditioning run. The previous
turn made progress through a verified source reproduction and UI correction;
its source calculations did not repair the failed learning mechanism.

## Question and complete fixed coverage

Determine whether the already recorded circuit supplies an actual DAN input
contrast while its KC history is still shared between matched taught and
untaught conditions. Use all 32 untaught fine histories and both home/away
contrasts for each: 64 comparisons, with original/second panels, eight games,
base/alternate noise and fixed recorded seeds. No selection by gain outcome,
classifier, distance threshold, routing coefficient, kinetic fit or new
teaching gate belongs to this analysis.

The frozen plan binds the helper/tests/driver, this contract, independent
reviews, six original source snapshots, current source counterparts, native
binary identity, graph/pilot prerequisites, both main archives and summaries,
recording layouts, capture receipt/terminal summary/progress, sample map,
cell atlas and all 32 fine archives. Every input hash and byte count is checked
before and after execution. Original files remain untouched.

The main records are the complete original and second `rate-bridge-v1` panels
`de050d773763` and `dea14759e9ca`. Fine inputs belong to the completed
`onset-history-capture-4343c21535c43f42`. This reuses observations; it does not
rerun that rejected onset-history candidate or count its findings as new.
Validate complete selectors and exact cue/noise/seed/protocol/source identities.
Frozen-plasticity rows are not substitute untaught inputs.

## Exact count and prefix contract

The implementation/source/recording proof is specified independently in
[the mathematical contract](causal-prefix-identifiability-contract-delivered-arrivals-2026-09-13.md)
and [the recording audit](dan-release-observability-evidence-ui-2026-09-13.md).
The source snapshots match recorded commit
`3b09865ba331b2c58c4bb8080e0d6354b4c8f612`; all six current source hashes also
match those original bytes at preparation. Pure-dopamine selected DAN rows
have exactly zero fast outgoing weights, while the current plasticity bridge
consumes only actual KC events and the compartment mean of actual DAN events.
All compared active runs reset identically and differ only in their requested
teaching schedule. Shared gain drift before teaching is permitted.

Decode each recorded float32 DAN mean by exact canonical float32(k/N)
encoding, with N=2 home and 22 away. Noncanonical adjacent float values and
negative zero are invalid, not approximately repaired. Check the full 2000-step
untaught pooled stream and KC totals against its fine binary per-cell raster;
check individual coarse DAN bins and all available cross-record identities.
Typed identities matter: Python's `True == 1 == 1.0` must not let boolean or
floating cue IDs masquerade as the declared integer IDs.

Let j be the first decoded DAN difference in either compartment. It must not
precede the first scheduled pulse at step 1550 (310 ms); a later difference may
be off the pulse grid and need not be positive. With no difference, record
null and the entire current trial's common prefix. The common-code induction
establishes identical non-DAN/KC spike selection through j inclusive. The gain
write happens after that selection. Compare observed total KC and grouped KC
state through `[0,j+1)` and rule/DAN filter states through `[0,j)`. Compare
individual KC filter endpoints and sampled coarse bins only when a whole bin
lies inside that inclusive prefix. A queue can already contain an earlier KC
event delivered using changed gains at j+1; the 1.8 ms delay supplies no extra
nine-step equality guarantee.

Do not infer identical individual taught DAN histories after the first pulse
from an equal population mean. Do not reuse untaught KC events after j, and do
not present the conditional current-rule prefix as a new nonlinear mechanism's
trajectory. Exact, complete inputs and initial local state would be needed to
exclude a deterministic input-only mechanism by a collision. One isolated
matched collision does not rule out a panel whose criterion averages cues.

## Required output and failure behavior

Preserve all 64 contrast rows, exact integer pooled count-difference arrays,
first divergence/counts and first observed KC/rule differences. Preserve each
common KC prefix's per-cell event counts/body IDs with its exact endpoint;
label those as conditional current-code equality, not a directly recorded
fine taught raster. Aggregate timing/count ranges by panel/noise/taught
condition descriptively. No scientific acceptance threshold is introduced.

The actual driver strictly validates float32 `(2000,5)` step signals,
integer KC totals 0..4064, unused zero columns, float64 grouped KC/rule
`(2000,8,2)/(2000,8,8)` records, float64 DAN `(2000,2,2)` records and individual
KC `(40,4064,2)` endpoints, integer `(40,24)` DAN bins, exact role maps and
binary int32 `(2000,4774)` fine events. All scheduled/sensory/checkpoint/mask
contracts must match; missing measurements cannot become zeros.

The output directory is exclusive. An invalid binding, malformed data,
incomplete matrix, failed parity or violated causal prefix stops the analysis,
retaining failed status, the error and already completed observations. No
restart, overwrite, adaptive retry or substitute dataset is allowed under the
same identity. A different implementation after a failure requires a new
reviewed identity with the original failed result preserved.

One externally supervised execution, 120 seconds through final input hashing
and summary persistence. The child observes the same finite cap; root's parent
must terminate and reap it on expiry, preserving stdout/stderr and actual
elapsed/exit state. No network, import of simulator/native/capture code, native
calls, neuronal runs or learning-rule evaluations are permitted.

## Consequence for the actual repair

A valid prefix contrast establishes an available local input distinction;
it does not choose receptor kinetics or demonstrate learning. A contradiction
instead identifies a specific source, identity or recording assumption to
repair before evaluating a new mechanism. The separately inspected release
model literature does not select a new calibrated PPL101/PAM12 release law.

The old criterion-3 prose's identical-D-only leakage inference is insufficient:
KC history and gain feedback can differ while D matches. Preserve the old
frozen document and numeric gate; record this as a new interpretation
correction. The actual untaught-home failure, both qualification requirements,
all accepted gains/interfaces/masks and the acquisition/reversal controls
remain unchanged. Full goal completion still requires those functional results.
