# Fixed DARELA event-transfer screen before circuit integration

Frozen scientific choices before any candidate evaluation of the 32 saved
untaught histories. Starting checkpoint: local `13966a2`. This is one
conditional rejection screen, not a native circuit experiment or a fitted
release model. The complete repair goal remains active.

## Question and source boundary

Does the already specified finite per-DAN event construction satisfy every
unchanged untaught-home/away guard on the two existing panels, with the
original spike histories held fixed? A valid failure rejects this particular
construction before production integration. A pass can justify further
native integration and qualification; it cannot demonstrate acquisition,
reversal, predictive value or correct recurrent activity under changed gains.
Both panels have been used previously and are not newly unseen data.

The pinned DARELA source, primary-paper transcription, source-kernel result,
independent mathematics, event helper/reference and bridge tests are those
committed in `13966a2` under
`docs/evidence/reward-mechanism-repair-2026-09-12`. The source is a mouse FSCV
burst model. Its Euler factor kernel and inclusive burst clock were checked
on eight fixed source cases. Old/new-H products were derived from the saved
factors; the full solver's post-H release ordering was established by code
inspection. Those cases will not be repeated. No source fit, alternative published
parameter row, fly calibration or intracellular receptor mechanism is
introduced by this screen.

## Frozen candidate and unchanged interfaces

Use only manuscript WT sweep-1 `p=(0.0105,-0.003,-0.0011)` and
`tau=(7.5,15,900)` seconds. At each actual spike of each DAN, emit
`a=product(H_before)` and then set `H_after=(1+p)*H_before`. Between that
body's events use exact exponential recovery toward one. Every trial resets
all H to one at time zero. The release helper remains byte-identical to the
verified implementation; its numerical reference uses 60-digit arithmetic.

Transform each of the 24 body histories before pooling with fixed divisors
two for PPL101/home and 22 for PAM12/away. Events before 100 ms advance H.
The existing bridge remains cold until step 500/100 ms: earlier release is
not injected retroactively. The release state emits nothing during silence;
the existing full analytic bridge tail accounts for all admitted impulses.
There is no burst detector, teaching-label gate, active-cell denominator,
baseline subtraction, clipping of release, or counterfactual event removal.

The admitted release domain is a reset 400 ms trial of 2,000 binary 0.2 ms
slots. Its conservative per-slot upper envelope is `2^32`, established
without observing these histories. It is validation of this finite domain,
not a state cap or an assertion of indefinite tonic stability. The bridge
accepts finite DAN masses in that envelope while KC masses remain binary
and within `[0,1]`.

Keep the 100 ms rate and 500 ms eligibility filters, eta 0.0005,
normalization 0.96, accepted gains, engineered ports, source graph, encoder,
readout and all original seeds unchanged. Double gains accumulate and clamp
within `[0.5,1.5]`; each complete electrical interval and the single full
tail publish float32 as before. Preserve 4,184 eligible home edges, 3,239
eligible gamma away edges, and 1,443 noneligible away edges at their original
unit checkpoint. No candidate gains are fed back into the neurons.

## Exact history and identity contract

Use the existing capture `onset-history-capture-4343c21535c43f42`, files
`fine_00.npz` through `fine_31.npz`, and the original
`onset-capture-preregistration.samples.npz` map. The main panels are
`diag-rate-bridge-v1-maskgamma-de050d773763` and
`diag-rate-bridge-v1-maskgamma-dea14759e9ca`.

The first panel's game IDs are 4362, 4366, 4370, 4374, 4378, 4383, 4387,
4391; the second's are 4395, 4399, 4404, 4408, 4412, 4416, 4420, 4425.
Ordering is panel, listed game, then base/alternate noise. Seeds are
`42+game+2,000,000*panel+1,000,000*alternate`, with panel and alternate in
`{0,1}`. Every captured trial is untaught and begins with unit gains.

Reuse the inspected v2 causal-prefix reader's strict metadata, source,
graph, pilot, selector and map identity checks. Its original-source sample
contract is int64 for main selectors and int32 for fine/native role indices;
do not coerce an incompatible archive to match a fixture. Verify the entire
fine numeric fingerprint, binary `int32(2000,4774)` raster, 4,064 KC/24 DAN/
686 sensory column partition, counts and reductions, empty imposed-pulse
arrays, and exact fine/main untaught float32 gain/delta parity. Source and
current native-code bytes remain bound but no native library is loaded.

The new machine plan must bind every consumed input, code file, imported
utility, test, source/parameter contract and preparation review before the
first candidate calculation. It retains the validated historical context
inputs plus the new frozen implementations. Derive a new identity from the
complete plan; do not reuse a historical identity. Any subsequent code or
protocol change requires another identity, preserving the earlier attempt.

## Retained outputs and acceptance arithmetic

For every history save original raw DAN events/counts, all per-body release
masses, H before and after each electrical event slot, the final recovery
endpoint at 400 ms, and pooled release. Retain global indices/body IDs,
channel and plastic-edge maps, original float32 gains, and the complete
candidate bridge outputs: final/electrical float32 and double gains,
per-edge and grouped phases, publication counts, bound contacts and endpoint
filter states. Hash each saved array archive and its semantic row identity.
Summary values never substitute for these arrays.

The eight guard cells are panel × noise × home/away. Each uses all eight
games and all eligible edges in that channel. Preserve the exact existing
integer-tick reduction `f32_trial_totals` and `point_guard`:
`abs(mean) <= 0.5*sample_SD`, including equality and zero-variance cases.
Do not replace sample SD with population SD or round descriptive means
before classifying. Report mean, sample SD, threshold and exact tick totals
for both original and candidate endpoints. Do not average away a failed
cell or stop once one fails.

Numerical validity requires finite positive release factors, zero emitted
mass on non-events, correct per-cell pooling, fixed maps, unchanged masked
gain bytes, and complete publication/phase/tail accounting. Electrical and
tail bound contacts are separately reported; any contact fails the necessary
screen even if the untaught guard passes. All 32 histories are evaluated
once unless invalid inputs, numerical failure or the watchdog prevent
completion; those failures preserve their partial artifacts.

## Independent audit and execution limits

Before execution, independently freeze the audit code and its contract.
Reconstruct all release/state outputs with the already verified 60-digit
reference, using `atol=rtol=1e-12`. Independently reconstruct the bridge
products, gain updates, clipping/publication, phases, groups and full tail.
Use the established complete pair-kernel reference as a separate check of
unclipped attempted integrals, not as a substitute for clipped gain history.
No audit may import the producer bridge as its sole numerical reference.

Freeze double endpoint allowance at `1e-11`; compare areas with absolute
`2e-11` plus relative `1e-12`, and endpoint filter states with combined
`1e-12`. Require exact final/electrical float32 endpoints where independently
resolved. Preserve any rounding ambiguity explicitly; do not force equality
or broaden tolerances after observing it. Exact tick guards must agree.
An ambiguity cannot be reported as a fully verified numerical pass.

The producer has one external 1,200-second whole-study cap, including input
validation, persistence and final checks. The independent saved-result audit
has its own external 600-second cap. Each child reserves time for persistence;
the parent kills and reaps on expiry. Observe existing live handles without
restarting an attempt because an observation wait elapsed. Completion needs
all 32 correctly ordered row/archive pairs, exact attempted/completed counts,
unchanged input bindings, matching identity/summary/completion hashes,
zero child exit, reaping, and no parent or child terminal-error marker.
Late completion markers do not override failures or deadlines.

Classify a complete, numerically verified all-eight-guard/no-bound-contact
result as `passed_necessary_untaught_screen`. Classify a valid guard or bound
failure as `rejected_fixed_hypothesis`. Invalid, incomplete or numerically
unresolved artifacts do not establish either full result. Do not rescue a
rejection by selecting another tau, p, onset, eta, mask, bound or threshold.

This phase changes no production/API/UI behavior or model pointer. The
existing failed qualification and `not_run_gate_failed` conditioning remain
accurate. Native integration, both complete qualification panels and the
controlled acquisition/reversal protocol remain required to complete the
user's goal. Do not push or promote. Every delegated agent personally reads
the entire narrative corpus through its current reading chain and all new
final additions; a manifest alone is not reading.
