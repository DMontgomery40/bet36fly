# KC local-state integration: minimum next implementation

At clean source checkpoint b86405d. The output-only Python wrapper implements the root's declared 30-Hz C/L/a/I map, not a native candidate. Final targeted verification: 115 writer cases plus 34 independent clock cases, 149 passed in 0.85 s; Ruff clean. The suite compares full trajectories with personally inspected, Git-blob-checked source functions, exercises controlled neighbor/recipient effects and old-state latency, checks rational frame partition/chunk invariance, and rejects invalid arithmetic before state publication. A red missing-module test preceded implementation; a later eight-case scalar-conversion overflow family failed before correction. No fitted parameters, actual histories or native calls were used.

The standalone API is `KCLocalState(params, recipient_csr)`, `consume(binary_KC_vector)`, `consume_chunk(binary_matrix)`, and `advance_boundary()`. Public snapshots expose `native_step`, `source_frames`, C/L/a/I, pending raw counts and susceptibility. Reading snapshots cannot change dynamics. Source parameters and adjacency are privately copied; source frames commit only after finite/nonnegative/order/admissibility validation. Empty chunks do not flush a boundary. A malformed chunk fails before consumption; an arithmetic failure preserves previously successful steps and leaves its offending step uncommitted.

| Surface | Minimum integration |
| --- | --- |
| `reward_protocol.py:162` / `:182` / `:233` | Derive incoming KC-local CSR from original retained contact counts in the established KC order; exclude self only in this new slow map, normalize incoming rows, preserve every electrical weight and eligibility flag. Bind map identity and explicit pan-KC assumption. |
| `reward_brain.py:28`, `:67`, `:161`, `:430` | Introduce a new named rule/config/layout and validated CSR ABI fields. Keep old modes unchanged. Allocate float64 C/L/a/I and int64 pending counts per KC plus integer source/native counters per call. Include every new recording buffer in allocation caps. If native code is split into headers, the build cache must hash those dependencies; its current cache key hashes only reward_lif.cpp. |
| `reward_lif.cpp:138`, `:175`, `:254` | Construct cold local state per simulate call; update it from time zero outside the gain-onset branch. Before each binary KC event, close due positive source boundaries using integer comparisons, then supply held L/C times that event at the existing KC bridge-input seam. Accumulate the unweighted event for local input. Apply the weighted event before both existing KC filters. Preserve raw endogenous DAN input, fast transmission at :181–190, and cold bridge onset. |
| `reward_lif.cpp:342` | Flush a completed local frame at the electrical endpoint for recording only. The existing analytic bridge tail remains valid because local calcium creates no events after electrical input ends. Keep double accumulation, float32 publication, bounds and checkpoint-only persistence. Local state and hidden double remainder do not cross trials. |
| `reward_teaching_diagnostic.py:130`, `:206`, `:264` | Bind the new contract, selected external-fit result, map, helper/native sources and recording schema before execution. Preserve all 64 conditions, four replays and 16 cumulative trials per panel, exact old selectors/seeds and seven criteria. Single trials reset gains; cumulative trials retain gains only. |

Record the source-frame input counts, C/L/a/I and held susceptibility by KC, exact frame endpoints, endpoint pending counts, per-class summaries (including other), and existing per-group signed/double/published/bound records. Fine binary KC history or a lossless event representation is needed to reconstruct fractional-clock frame membership; the existing 10-ms sampled counts alone cannot do that. A packed 2000×4064 binary KC raster is 1,016,000 bytes per 400-ms trial before compression, not an added electrical simulation. Record complete numerical fingerprints for selected repeated calls and use independent recomputation for local states and gains.

The diagnostic/artifact/UI integration is necessary before presenting a candidate verdict. `reward_evidence.py:148` currently recognizes only event/bridge; `:404` requires the exact old replay fields; `:428` validates only the old layout, and `:228` reads historical locks from a bridge-specific evidence file. Add a separately identified local-rule verifier/lock inventory without rewriting old evidence. `conditioning_runner.py:59` and `:1024`, plus conditioning numerical/compact schemas, must accept the new rule only with its full qualified pair and current dependencies. The two old rule enums/layouts must not be silently reinterpreted.

Update `docs/api-contract.md:108`, `web/src/types.ts:43` and `RewardEvidence.tsx:23` together: show the new local operator/30-Hz clock, external parameter/map identity, state lifetime and unchanged bridge tail under its own rule. Keep stored versus independently validated verdicts separate and conditioning unrun until both complete live panels pass. A Python synthetic pass or later saved-history shadow cannot supply that qualification. Native parity, recorder/off/mask/fast-transmission invariance and affected API/browser acceptance remain future work, owned by the coordinated production task.
