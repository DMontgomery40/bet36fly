# KC local-state shadow runner preparation

The output-only runner is implemented and synthetically tested. No plan has been prepared and no saved-history candidate evaluation has run. Root owns review, the external watchdog and any later dispatch.

`run_kc_local_shadow.py` preserves the exact 32 ordered panel/game/noise/seed selectors and the original 8,866-edge mapping. It requires both successful bounded parent/child external-fit records and the independent fixed-parameter source audit, checks their numerical parameter agreement, and binds the source files, fit artifacts, contact map, atlas, all captures, contract, implementation and review/auditor files. It imports the ordinary weighted-event bridge; it does not import the rejected weight-dependent rule or any engine, optimizer or network client.

Each trial resets local state, consumes all 2,000 original binary KC frames on the exact rational clock, flushes the twelfth source frame at the electrical endpoint, and applies held L/C to both cold-onset KC bridge filters. Original endogenous DAN population means, masks, gains, clocks and complete analytic tail remain fixed. The stored source arrays are `local_frame_states` (13,4,4064; C,L,a,I) and `source_frame_counts` (12,4064), with susceptibility and raw/weighted per-KC totals. Per-edge/group phase areas, F64/F32 electrical/final gains, publication counts, both bound counts, original comparison gains and all four subtype buckets are retained.

All eight half-SD guards use exact float32 integer trial totals and the existing point predicate. A successful computation remains a fixed-history rejection screen pending independent saved-artifact audit; it cannot qualify a circuit. Any invalid row stops the whole attempt, preserves completed artifacts and journal outcomes, and reports incomplete/invalid. The 1,200-second alarm includes final persistence, with a ten-second processing reserve. An exclusive output directory prevents reuse, and any terminal-write error invalidates the completion receipt.

Verification: 228 runner cases passed in 1.43 seconds. The combined 428-case runner/local-state/independent-clock/bridge/composition suite passed in 3.08 seconds; Ruff passed. Cases cover every selector field, full ordering/types, fit convergence and parent watchdog, parameter/artifact disagreement, invalid local domains, exact endpoint recording, source/persistence failures, prefix preservation, output reuse, strict JSON, pre-allocation sparse headers and both final-write clock boundaries. Read-only input preflight verified the actual fit/map/atlas identity, including 1,557 gamma, 695 alpha-prime/beta-prime, 1,810 alpha/beta and two other KCs. That preflight processed no capture raster through a candidate.

Production/API/Training remain unchanged and accurately report the existing circuit evidence. Root owns the repository-wide verification; this output-only task did not duplicate native, browser or server checks.
