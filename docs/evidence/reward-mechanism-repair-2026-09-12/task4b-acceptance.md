# Mechanism evidence API and Training view acceptance

September 13, 2026 UTC. The evidence reader and Training view now distinguish
stored verdicts from independently reconstructed criteria. The original final
bridge panel is validated passed, its second panel validated failed, and the
older raw pair remains overall unverified because complete replay/sensory
evidence was not retained. Conditioning and reversal have not run. This closes
the current diagnostic evidence display work, not the scientific repair.

The [implementation report](task4b-evidence-ui-report.md) records the fixes:
complete scientific pairing, summary/archive locks, finite/type-safe metadata,
full matrix/replay/sensory validation, bridge group/endpoint/tail accounting,
cache and detail replacement races, and truthful loading/error/stale states.
Tests reproduce broader corruption and state-transition families, including
rendering actual endpoint payloads through the real React component. The reader
does not invoke the neural simulator. Future conditioning manifests still lack
an independent scientific artifact validator and cannot acquire a pass from
execution status or stored flags.

The [independent review](delivered-arrivals-resumed-evidence-review-final.md)
rechecked its three concrete findings and found no remaining actionable issue
within that scope: 12 endpoint/group probes, 24 malformed-field React renders,
all four blocker states and eight focused regression tests passed. These
counterexamples harden artifact validation; they do not change the preserved
scientific outcomes. Its [copy manifest](task4b-review-copy-manifest.json)
retains the initial findings and executable probes alongside the final review.

Root ran [the full repository gate](task4b-root-verify.txt) on the final
[released files](task4b-release-hashes.json): **970 Python tests, 81 frontend
tests, Ruff and the production build passed**. Python reported two existing
deprecation warnings. The writer independently ran the same gate before release.
The cell atlas check also passed; its three anatomical tables require no
simulation, and the updated atlas metadata changes only the protocol source hash.

Root started the explicit read-only factory with bytecode writes disabled on
assigned localhost port 52861, PID 70057. The guarded
[actual browser workflow](bet36fly-reward-browser-evidence.json) passed:

- Four current diagnostic detail views match their API validation, seven
  criteria and exact displayed guard measurements. Raw NPZ downloads return 404.
- The bridge pair visibly fails; conditioning explicitly says it has not run.
- Sixteen labeled synthetic scenarios cover loading, empty, errors, incomplete,
  running, cancellation, budget stops, unknown states and raw-mask presentation.
  They include actual Retry recovery and successful→retained-stale→recovered
  transitions for mechanism and conditioning requests; no fixtures were saved
  as measured experiments.
- Eight historical sports downloads match their recorded hashes; the three
  historical arms remain selectable. Active v1 and paused v2 retain their states.
- Widths 320, 390, 540, 720 and 1024 pixels have no page overflow. No JavaScript
  errors were recorded. Root inspected the [mechanism overview](bet36fly-mechanism-desktop.png)
  and [failed bridge detail](diag-rate-bridge-v1-maskgamma-dea14759e9ca-detail.png).

The [baseline](task4b-file-baseline.json) covers **782 files / 5,497,585,613
bytes**, including data/ledgers, current model, historical experiments, diagnostic
archives, native libraries, source/bytecode, rebuilt frontend and the copied
Microduck snapshot. Both [after-request](task4b-files-after-requests.json) and
[after-shutdown](task4b-files-after-shutdown.json) checks found no changed bytes,
added entries or removed entries. The owned QA server stopped with exit code 0;
unrelated servers were left alone. The [copy manifest](task4b-copy-manifest.json)
records preserved output sources and the extended file-check scope.

This verification does not undo the [earlier normal-startup incident](verification-startup-incident.md)
or claim all prior live state was untouched. No new circuit experiment, sports
pilot, promotion or push occurred during this evidence/UI acceptance.
