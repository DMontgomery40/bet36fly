# Verification — wiki research integration, September 14, 2026 UTC

This task integrated research documentation. It did not change the simulator, protocol, API or frontend, launch a neural experiment, inspect the remote confirmation job, promote a model, push or deploy. Existing application descriptions were checked against the implemented routes/readouts and linked evidence; no new browser acceptance is claimed.

## Completed checks

- `make verify`: **4,191 pytest tests passed**, with two warnings; Ruff passed; **144 frontend tests across nine files passed**; TypeScript/Vite production build passed. Complete execution finished September 14 UTC (September 13, 23:57 Denver). These are software checks, not new evidence of biological learning or prediction.
- `.venv/bin/python scripts/export_fly_cell_atlas.py --check`: passed; generated legacy atlas and body identities remain consistent with the exporter.
- `.venv/bin/python docs/evidence/wiki-integration-2026-09-14/audit_anatomy.py`: completed. The read-only audit covers the complete source-aligned graph, all rows in the three saved recovery season records, their checkpoint inventories, selected bodies and eligible-gain denominators. [Reproducible output and source hashes](anatomy-and-storage.json).
- Markdown cross-links, heading anchors, authored-page navigation, metadata, protected-file hashes and whitespace: recorded in the [documentation checks](documentation-checks.json).
- Read all 23 pre-existing authored wiki pages; reconcile their current claims with the four new topic pages, later handoff queue, primary-source ledger and frozen results. [Coverage and corrections](reconciliation.md).

## Boundaries

Of 142 protected files, 141 are byte-preserved against the pre-edit SHA-256 inventory, including imported sources, frozen associative/sensory evidence, configurations, active pointer and unrelated public-post handoff. The sole difference is the original proposal memo: concurrent Claude commit `881864d` added two lines linking the new static audit. Its research body is unchanged, and this task did not edit that memo. The checker verifies the current memo bytes against that commit and records both hashes. The new static audit files were committed by the concurrent documentation writer in `881864d`; their content was re-read and retained. This task's later local commit contains the integrated wiki and supporting documentation.

No 2018 confirmation result was locally recorded at the checked cutoff. Remote status and future research proposals remain outside this task. Browser checks from earlier implementation tasks remain dated evidence, not tests rerun by this documentation integration.
