# Chronological MLB data eligibility — fixed before retrieval

Development retrieval covers 2019–2022 only. Confirmation is 2023 regular season, and its source may not be accessed until the final candidate and confirmation freeze are written. No 2024+ information enters this task's features.

Fetch the public MLB Stats API schedule for each full season with sportId=1 and gameType=R. Preserve raw bytes, exact URL, UTC retrieval time and SHA256 in a separate sensory source directory. Keep only gameType R, abstractGameState Final, exact integer nonnegative unequal scores, distinct integer team IDs, exact integer gamePk, and a timezone-bearing gameDate in the requested calendar year. Exclude a game ID entirely if any returned occurrence has a nonempty key containing 'resume' or a suspended detailed state; the endpoint does not establish its historical completion time. Exclude non-final/cancelled/postponed/non-regular and tied fixtures with counts. Duplicate eligible IDs fail even if identical; inconsistent final score/winner flags fail. No silent deduplication. The season response must contain games; an empty or malformed source fails.

The normalised rows retain stable numeric MLB team IDs (names are display only), season, source identity and exclusion reasons. Feature state advances at UTC-day boundaries with a conservative 48-hour delay from original scheduled start, excluding all resumed games. This follows the existing repo feature function. Retrospective schedule corrections and unobserved exact historical publication times remain limits. Same-day games do not update one another, and no final score enters its own feature row. Training transforms fit 2019–2021 only; 2022 is development, 2023 remains protected.

Fetch authorization does not authorize neural or fitting expansion before sensory/two-probe acceptance. Training/development source acquisition and validation can proceed independently. No bookmaker/account data or spending is involved.
