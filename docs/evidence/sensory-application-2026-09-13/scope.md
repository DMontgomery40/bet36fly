# Sensory application replacement

Presentation identity: `sensory-application-01`, around unchanged confirmation `sensory-confirmation-4887cb8c17f6281d8166`.

This task integrates existing results. It makes no new choice of neural dynamics, stimulation, readout parameters or scientific acceptance thresholds. It does not rerun or refit the confirmation. The existing qualified claim remains conditional second-order sensory response and complete fitted historical pipeline performance.

Read and reconciled the current handoff, direction, model card, API contract, authored wiki/cell atlas, relevant imported MaleCNS/interface research, final result, complete confirmation CSV and JSON artifacts, and the actual encoder, cache construction, probe schedule and native output aggregation code. The new adapter checks all 2,423 CSV rows and all candidate response entries; each request rechecks presentation hashes. Preserved source anchors and protocol choices are attributed to their frozen artifacts.

A bounded September 13 source recheck opened the Shiu repository README (FlyWire model, 47 commits shown, one issue, no named release displayed). The MaleCNS release page could not be opened by the web tool, and Cameron's PMC page returned a browser challenge. These retrieval limitations do not alter the previously frozen source records; no new physiological parameter is inferred. Source URLs: https://male-cns.janelia.org/release/, https://github.com/philshiu/Drosophila_brain_model, https://pmc.ncbi.nlm.nih.gov/articles/PMC2865571/. The imported research and frozen source package remain unchanged. This is not a new community reproduction or exhaustive literature check.

The mounted product has three pages and no legacy polling, predictions, training controls or brain animation. Rate charts show current output cells and stored measurements; the adapter explicitly omits achieved per-cell input rates. Historical compatibility endpoints remain separate. Default app startup no longer constructs a legacy runtime or starts source-following; explicit compatibility tests initialize it themselves.

The occupied local port 8765 was left alone. QA uses an isolated `make serve-verify QA_PORT=8766` process. Browser plugin was unavailable; the existing bundled Playwright Chromium supplies browser acceptance. Synthetic failure/loading states are test fixtures, not experimental results. Screenshots and logs are kept under `/tmp`.
